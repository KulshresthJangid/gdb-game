const AUTH_ROUTES = {
    LOGIN: '/api/v1/login',
    REGISTER: '/api/v1/register',
    OTP_VERIFY: '/api/v1/otp-verify',
    TOKEN_VERIFY: '/api/v1/token-verify',
}

const TABLES = {
    USERS: 'users'
}

module.exports = {
    AUTH_ROUTES, 
    TABLES
}